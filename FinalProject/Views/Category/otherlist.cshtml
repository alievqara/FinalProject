@model IEnumerable<OtherProduct>

@{
    ViewData["Title"] = "Other Product List";
    int counter = 1;
}

<div class="container-fluid pt-4 px-4">
    <div class="row g-4">
        <div class="col-xl-12">
            <div class="card">
                <div class="card-body">

                    <div class="d-flex align-items-center justify-content-between m-2">
                        <h6 class="mb-4">Category List Table</h6>
                        <a class="btn btn-outline-light" asp-action="Create">Create New</a>
                    </div>
                    <div asp-validation-summary="ModelOnly"></div>
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th scope="col">№</th>
                                <th scope="col">Product Brand Name</th>
                                <th scope="col">Product Model Name</th>
                                <th scope="col">Product  BarCode</th>
                                <th scope="col">Product  Detail</th>
                                <th scope="col">Created Date</th>
                                <th scope="col">Creator Name</th>
                                <th scope="col">Updated Date</th>
                                <th scope="col">Updator Name</th>
                                <th scope="col">Settrings</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (OtherProduct otherProduct in Model)
                            {
                                <tr>
                                    <th scope="row">@(counter++)</th>
                                    <td>@otherProduct.Brand_Name</td>
                                    <td>@otherProduct.Model_Name</td>
                                    <td>@otherProduct.Barcode</td>
                                    <td>@otherProduct.Detail</td>
                                    <td>@otherProduct.CreatedTime.ToString()</td>
                                    <td>@otherProduct.CreatorName</td>
                                    <td>@otherProduct.EditedTime.ToString()</td>
                                    <td>@otherProduct.EditorName</td>
                                    <td>
                                        <a class="btn btn-sm btn-outline-warning" asp-action="Update" asp-route-id="@otherProduct.Id">Update</a>
                                        <a class="btn btn-sm btn-outline-primary" asp-action="Delete" asp-route-id="@otherProduct.Id">Delete</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>